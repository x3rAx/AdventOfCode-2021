{
    "configurations": [
        {
            "name": "Current Day",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/.vscode/launcher.py",
            "args": [ "${workspaceFolder}/src", "${file}" ],
            "console": "integratedTerminal",
        }
    ]
}
